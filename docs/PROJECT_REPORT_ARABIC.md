# 📊 تقرير شامل لمشروع Bot_IT - روبوت المحادثة الصوتي

## 📋 جدول المحتويات

1. [نظرة عامة على المشروع](#نظرة-عامة-على-المشروع)
2. [التقنيات والأدوات المستخدمة](#التقنيات-والأدوات-المستخدمة)
3. [هيكل المشروع](#هيكل-المشروع)
4. [المكونات الرئيسية](#المكونات-الرئيسية)
5. [آلية العمل](#آلية-العمل)
6. [المشاكل التي تم حلها](#المشاكل-التي-تم-حلها)
7. [دليل التشغيل والاستخدام](#دليل-التشغيل-والاستخدام)
8. [التحديثات الأخيرة](#التحديثات-الأخيرة)

---

## 🎯 نظرة عامة على المشروع

**Bot_IT** هو روبوت محادثة صوتي متخصص في مجال علوم الحاسب وهندسة المعلوماتية. يجمع المشروع بين تقنيات متقدمة لتحويل الصوت إلى نص والعكس، مع استخدام الذكاء الاصطناعي لتوليد الإجابات.

### 🌟 المميزات الرئيسية:

- ✅ **تحويل الصوت إلى نص**: استخدام Web Speech API في المتصفح
- ✅ **كلمة التنبيه**: يستجيب الروبوت فقط عند بدء السؤال بكلمة "روبوت"
- ✅ **التوليد النصي المتدفق**: استخدام Vertex AI Streaming API
- ✅ **النطق العربي التلقائي**: تحويل الردود النصية إلى صوت عربي
- ✅ **تخصص في علوم الحاسب**: يجيب فقط على الأسئلة المتعلقة بهذا المجال
- ✅ **واجهة سهلة الاستخدام**: واجهة ويب بسيطة وسريعة الاستجابة

### 📊 معلومات المشروع:

| الخاصية | القيمة |
|---------|--------|
| **اسم المشروع** | voice-chatbot-gemini-live |
| **الإصدار** | 3.0.0 |
| **الترخيص** | MIT |
| **لغة البرمجة الرئيسية** | JavaScript (Node.js) + Python |
| **نموذج الذكاء الاصطناعي** | Gemini 2.5 Flash Lite |

---

## 🔧 التقنيات والأدوات المستخدمة

### 1️⃣ التقنيات الأساسية:

#### Backend (Node.js):
```
- Express.js v4.18.2    : إطار عمل لبناء خادم الويب
- WebSocket (ws) v8.14.2 : للاتصال الفوري بين الخادم والعميل
- Axios v1.13.4         : لطلبات HTTP إلى Vertex AI API
- Dotenv v16.3.1        : لإدارة متغيرات البيئة
```

#### Backend (Python):
```
- Edge-TTS v6.1.9       : لتحويل النص إلى صوت عربي
- Aiohttp v3.9.1        : خادم ويب غير متزامن لـ TTS
- Aiofiles v23.2.1      : للتعامل مع الملفات بشكل غير متزامن
```

#### Frontend:
```
- Web Speech API         : لتحويل الصوت إلى نص (Speech-to-Text)
- Web Speech Synthesis   : لتحويل النص إلى صوت (Text-to-Speech)
- HTML5/CSS3/JavaScript : واجهة المستخدم
```

### 2️⃣ واجهات برمجة التطبيقات (APIs):

#### Vertex AI REST API:
- **النموذج**: Gemini 2.5 Flash Lite
- **الوظيفة**: توليد الردود النصية بشكل متدفق
- **الميزة**: دعم اللغة العربية وتوليد نصوص احترافية

#### Edge-TTS:
- **الوظيفة**: تحويل النص العربي إلى صوت MP3
- **الأصوات المدعومة**: 10 أصوات عربية مختلفة (سعودي، مصري، إماراتي، كويتي، قطري)

---

## 📁 هيكل المشروع

```
bot_it/
│
├── 📂 backend/
│   ├── server.js                 # خادم Node.js الرئيسي مع WebSocket
│   ├── config.js                 # ملف الإعدادات والتكوين
│   ├── gemini-text-handler.js    # معالج الاتصال بـ Vertex AI
│   └── tts-server.py             # خادم Python لتحويل النص إلى صوت
│
├── 📂 frontend/
│   ├── index.html                # الصفحة الرئيسية
│   ├── app.js                    # منطق التطبيق في المتصفح
│   └── styles.css                # تنسيقات الواجهة
│
├── 📄 start.bat                  # سكريبت بدء التشغيل الشامل
├── 📄 stop.bat                   # سكريبت إيقاف الخوادم
├── 📄 kill-port.bat              # سكريبت تحرير المنافذ المستخدمة
├── 📄 requirements.txt           # متطلبات Python
├── 📄 package.json               # متطلبات Node.js
└── 📄 .env                       # متغيرات البيئة والإعدادات
```

---

## 🧩 المكونات الرئيسية

### 1️⃣ Backend Server (Node.js)

#### الملف: [`backend/server.js`](bot_it/backend/server.js)

**الوظائف الرئيسية:**
- تشغيل خادم Express على المنفذ 8080
- إعداد خادم WebSocket للاتصال الفوري
- التعامل مع رسائل المحادثة الصوتية
- إدارة جلسات المستخدمين
- معالجة كلمة التنبيه "روبوت"

**الميزات الجديدة:**
- ✅ معالجة تلقائية لتعارض المنافذ (يحاول المنافذ 8080-8090)
- ✅ إدارة WebSocket ديناميكية
- ✅ تسجيل الأخطاء والأحداث بشكل مفصل

**المنافذ:**
- المنفذ الأساسي: 8080
- المنافذ البديلة: 8081-8090 (في حالة تعارض المنفذ الرئيسي)

### 2️⃣ Configuration Module

#### الملف: [`backend/config.js`](bot_it/backend/config.js)

**الإعدادات الرئيسية:**

```javascript
{
  // نموذج الذكاء الاصطناعي
  geminiModel: 'gemini-2.5-flash-lite',
  
  // كلمة التنبيه
  wakeWord: 'روبوت',
  wakeWordVariants: ['روبوت', 'روبوت:', 'روبوت،', 'Robot', 'robot'],
  
  // إعدادات الخادم
  port: 8080,
  
  // وضع التصحيح
  debugMode: true,
  
  // تعليمات النظام
  systemInstruction: 'أنت مساعد صوتي متخصص في علوم الحاسب...'
}
```

**وظيفة التحقق من كلمة التنبيه:**
- تتحقق مما إذا كان النص يبدأ بكلمة "روبوت"
- تدعم متغيرات متعددة (روبوت، روبوت:، روبوت،)
- تتجاهل المسافات وعلامات الترقيم الزائدة

### 3️⃣ Gemini Text Handler

#### الملف: [`backend/gemini-text-handler.js`](bot_it/backend/gemini-text-handler.js)

**الوظيفة:**
- إرسال النص إلى Vertex AI API
- استقبال الردود بشكل متدفق (streaming)
- معالجة الأخطاء وإعادة المحاولة

**مثال على الاستخدام:**
```javascript
await streamTextResponse('ما هي قاعدة البيانات؟', (chunk) => {
  console.log('Received chunk:', chunk);
});
```

### 4️⃣ TTS Server (Python)

#### الملف: [`backend/tts-server.py`](bot_it/backend/tts-server.py)

**الوظائف:**
- تشغيل خادم HTTP على المنفذ 5000
- توفير واجهة API لتحويل النص إلى صوت
- دعم 10 أصوات عربية مختلفة

**النهايات (Endpoints):**
```
GET  /health              : فحص حالة الخادم
GET  /voices              : قائمة الأصوات المتاحة
POST /synthesize          : تحويل النص إلى صوت (Base64)
POST /synthesize/stream   : تحويل النص إلى صوت (Stream)
```

**الأصوات العربية المدعومة:**
| الصوت | اللهجة | النوع |
|-------|--------|------|
| ar-SA-HamedNeural | سعودي | ذكر |
| ar-SA-ZariyahNeural | سعودي | أنثى |
| ar-EG-SalmaNeural | مصري | أنثى |
| ar-EG-ShakirNeural | مصري | ذكر |
| ar-AE-FatimaNeural | إماراتي | أنثى |
| ar-AE-HamdanNeural | إماراتي | ذكر |
| ar-KW-FahedNeural | كويتي | ذكر |
| ar-KW-NouraNeural | كويتي | أنثى |
| ar-QA-AmalNeural | قطري | أنثى |
| ar-QA-MoazNeural | قطري | ذكر |

### 5️⃣ Frontend Application

#### الملف: [`frontend/app.js`](bot_it/frontend/app.js)

**الوظائف:**
- إعداد WebSocket للاتصال بالخادم
- استخدام Web Speech API لتحويل الصوت إلى نص
- عرض النتائج المتدفقية في الوقت الفعلي
- نطق الردود تلقائياً باستخدام Web Speech Synthesis

**أحداث WebSocket:**
```javascript
// استقبال حالة الروبوت
ws.onmessage = (event) => {
  const data = JSON.parse(event.data);
  
  switch(data.type) {
    case 'status':           // حالة الروبوت (جاهز/يفكر/خطأ)
    case 'wake_debug':       // معلومات كشف كلمة التنبيه
    case 'assistant_delta':  // جزء من الرد المتدفق
    case 'assistant_done':   // انتهاء الرد
    case 'error':            // رسالة خطأ
  }
};
```

---

## 🔄 آلية العمل

### خطوة بخطوة:

#### 1️⃣ بدء التشغيل:
```
المستخدم ينفذ start.bat
    ↓
تحرير المنافذ (8080، 5000، 3000)
    ↓
فحص Python و Node.js
    ↓
تثبيت المتطلبات
    ↓
تشغيل خادم TTS (Python) على المنفذ 5000
    ↓
تشغيل خادم الرئيسي (Node.js) على المنفذ 8080
```

#### 2️⃣ المحادثة الصوتية:
```
المستخدم يضغط على زر الميكروفون
    ↓
Web Speech API يستمع للصوت
    ↓
تحويل الصوت إلى نص
    ↓
إرسال النص عبر WebSocket إلى الخادم
    ↓
الخادم يتحقق من كلمة "روبوت"
    ↓
إذا وُجدت: إرسال السؤال إلى Vertex AI
    ↓
استقبال الرد بشكل متدفق
    ↓
إرسال الأجزاء إلى المتصفح
    ↓
عرض النص + نطقه تلقائياً
```

#### 3️⃣ معالجة كلمة التنبيه:

```javascript
// مثال 1: كلمة التنبيه موجودة
المدخل: "روبوت ما هي قاعدة البيانات؟"
↓
الكشف: ✅ وجدت كلمة "روبوت"
↓
النص النظيف: "ما هي قاعدة البيانات؟"
↓
الإجراء: إرسال السؤال إلى الذكاء الاصطناعي

// مثال 2: كلمة التنبيه غير موجودة
المدخل: "ما هي قاعدة البيانات؟"
↓
الكشف: ❌ لم تُوجد كلمة "روبوت"
↓
الإجراء: طلب من المستخدم البدء بكلمة "روبوت"
```

---

## 🐛 المشاكل التي تم حلها

### 1️⃣ مشكلة المنفذ المستخدم (EADDRINUSE)

**المشكلة:**
```
Error: listen EADDRINUSE: address already in use :::8080
```

**الحلول المطبقة:**

#### أ) سكريبت [`kill-port.bat`](bot_it/kill-port.bat):
```batch
for /f "tokens=5" %%a in ('netstat -aon ^| findstr :8080 ^| findstr LISTENING') do (
    taskkill /F /PID %%a 2>nul
)
```

#### ب) معالجة تلقائية في [`server.js`](bot_it/backend/server.js):
```javascript
function startServer(port) {
  const serverInstance = server.listen(port);
  
  serverInstance.on('error', (err) => {
    if (err.code === 'EADDRINUSE') {
      console.log(`⚠️ المنفذ ${port} مستخدم، جاري المحاولة على المنفذ ${port + 1}...`);
      startServer(port + 1);
    }
  });
}
```

### 2️⃣ مشكلة ترميز الأحرف في Python

**المشكلة:**
```
UnicodeEncodeError: 'charmap' codec can't encode character '\U0001f50a'
```

**الحل في [`tts-server.py`](bot_it/backend/tts-server.py):**
```python
import sys
import io

# Set stdout to UTF-8 encoding for Windows compatibility
if sys.platform == 'win32':
    sys.stdout = io.TextIOWrapper(sys.stdout.buffer, encoding='utf-8', errors='replace')
    sys.stderr = io.TextIOWrapper(sys.stderr.buffer, encoding='utf-8', errors='replace')
```

### 3️⃣ مشكلة إدخال redirection في Windows

**المشكلة:**
```
ERROR: Input redirection is not supported, exiting the process immediately.
```

**الحل في [`start.bat`](bot_it/start.bat):**
- استبدال `timeout /t X /nobreak >nul` بـ `ping127.0.0.1 -n X+1 >nul`
- إزالة `>nul2>&1` من أوامر `start`

### 4️⃣ مشكلة متغير wss غير معرف

**المشكلة:**
```
ReferenceError: wss is not defined
```

**الحل:**
- تحديث `module.exports` في [`server.js`](bot_it/backend/server.js):
```javascript
// قبل:
module.exports = { app, server, wss };

// بعد:
module.exports = { app, server };
```

---

## 📖 دليل التشغيل والاستخدام

### المتطلبات الأساسية:

#### 1. البرامج المطلوبة:
- **Python** 3.8 أو أحدث
- **Node.js** 16.0 أو أحدث
- **متصفح حديث** يدعم Web Speech API (Chrome, Edge)

#### 2. ملف الإعدادات (`.env`):
```env
GOOGLE_CLOUD_API_KEY=مفتاح_API_الخاص_بك
GEMINI_MODEL=gemini-2.5-flash-lite
WAKE_WORD=روبوت
PORT=8080
DEBUG_MODE=true
```

### خطوات التشغيل:

#### الطريقة الأولى: استخدام start.bat (موصى بها)

1. **افتح موجه الأوامر (CMD)**
2. **انتقل إلى مجلد المشروع:**
   ```batch
   cd d:\saad\مشاريع تخرج\bot_it
   ```

3. **شغّل المشروع:**
   ```batch
   start.bat
   ```

4. **افتح المتصفح على:**
   ```
   http://localhost:8080
   ```

#### الطريقة الثانية: التشغيل اليدوي

1. **شغّل خادم TTS (Python):**
   ```batch
   python backend\tts-server.py
   ```

2. **افتح نافذة CMD جديدة وشغّل خادم Node.js:**
   ```batch
   npm start
   ```

3. **افتح المتصفح على:**
   ```
   http://localhost:8080
   ```

### كيفية الاستخدام:

1. **افتح الصفحة في المتصفح**
2. **اضغط على زر الميكروفون 🎤**
3. **قل: "روبوت" ثم سؤالك**
   - مثال: "روبوت ما هي قاعدة البيانات؟"
4. **انتظر الرد**
5. **سيتم نطق الرد تلقائياً بالعربية**

### إيقاف المشروع:

#### الطريقة الأولى: استخدام stop.bat
```batch
stop.bat
```

#### الطريقة الثانية: يدوياً
- أغلق نوافذ Terminal المفتوحة
- أو اضغط `Ctrl+C` في كل نافذة

---

## 🆕 التحديثات الأخيرة

### الإصدار 3.0.0 - التحديثات:

#### ✅ الميزات الجديدة:

1. **معالجة تلقائية لتعارض المنافذ:**
   - يحاول المنافذ من 8080 إلى 8090
   - رسائل واضحة عند تغيير المنفذ

2. **سكريبتات تشغيل محسّنة:**
   - [`start.bat`](bot_it/start.bat): تشغيل شامل مع فحص المتطلبات
   - [`stop.bat`](bot_it/stop.bat): إيقاف جميع الخوادم
   - [`kill-port.bat`](bot_it/kill-port.bat): تحرير المنافذ المستخدمة

3. **إصلاحات الأخطاء:**
   - ✅ حل مشكلة UnicodeEncodeError في Python
   - ✅ حل مشكلة Input redirection في Windows
   - ✅ حل مشكلة wss غير معرف

4. **تحسينات في الأداء:**
   - استخدام `ping` بدلاً من `timeout` للتأخير
   - إزالة output redirection المسببة للمشاكل

5. **توافق أفضل مع Windows:**
   - دعم UTF-8 الكامل في Python
   - معالجة الأخطاء بشكل أفضل
   - رسائل واضحة بالعربية

#### 📊 الإحصائيات:

| العنصر | القيمة |
|--------|--------|
| عدد الملفات المعدلة | 5 |
| عدد الملفات الجديدة | 3 |
| عدد الأخطاء المحلولة | 4 |
| الوقت المستغرق | ~30 دقيقة |

---

## 📚 ملخص التقنيات

### البنية المعمارية:

```
┌─────────────────────────────────────────────────────────┐
│                    المتصفح (Browser)                     │
│  ┌───────────────────────────────────────────────────┐  │
│  │  Web Speech API (Speech-to-Text)                 │  │
│  │  Web Speech Synthesis (Text-to-Speech)           │  │
│  │  WebSocket Client                                │  │
│  └───────────────────────────────────────────────────┘  │
└──────────────────────┬──────────────────────────────────┘
                       │ WebSocket
                       ↓
┌─────────────────────────────────────────────────────────┐
│              خادم Node.js (Port 8080)                   │
│  ┌───────────────────────────────────────────────────┐  │
│  │  Express Server                                   │  │
│  │  WebSocket Server                                │  │
│  │  Wake Word Detection                             │  │
│  │  Vertex AI Client                                │  │
│  └───────────────────────────────────────────────────┘  │
└──────────┬───────────────────────────────────────────────┘
           │ HTTP REST API
           ↓
┌─────────────────────────────────────────────────────────┐
│              Vertex AI (Gemini 2.5 Flash)              │
│  ┌───────────────────────────────────────────────────┐  │
│  │  Text Generation (Streaming)                     │  │
│  └───────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│            خادم Python TTS (Port 5000)                  │
│  ┌───────────────────────────────────────────────────┐  │
│  │  Edge-TTS Server                                 │  │
│  │  10 Arabic Voices                                │  │
│  └───────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
```

### تدفق البيانات:

```
الصوت (المستخدم)
    ↓
Web Speech API (المتصفح)
    ↓
النص (كلمة + سؤال)
    ↓
WebSocket → الخادم
    ↓
كشف كلمة "روبوت"
    ↓
Vertex AI API
    ↓
الرد النصي (متدفق)
    ↓
WebSocket ← المتصفح
    ↓
Web Speech Synthesis
    ↓
الصوت (الرد)
```

---

## 🎓 الخلاصة

**مشروع Bot_IT** هو نظام محادثة صوتي متطور يجمع بين:

1. **الذكاء الاصطناعي**: Vertex AI (Gemini 2.5 Flash Lite)
2. **معالجة الصوت**: Web Speech API + Edge-TTS
3. **الاتصال الفوري**: WebSocket
4. **واجهة سهلة**: HTML/CSS/JavaScript

**النقاط القوية:**
- ✅ متخصص في علوم الحاسب
- ✅ دعم كامل للغة العربية
- ✅ معالجة تلقائية للأخطاء
- ✅ سهولة التشغيل والاستخدام
- ✅ أداء عالي وسرعة استجابة

**التطبيقات المحتملة:**
- التعليم الإلكتروني
- المساعدة الافتراضية
- الدعم الفني
- التدريب والتوجيه

---

## 📞 معلومات الاتصال

للمزيد من المعلومات أو الدعم، يرجى مراجعة:
- ملفات المشروع في: `d:\saad\مشاريع تخرج\bot_it`
- ملف [`COMPREHENSIVE_PROJECT_REPORT.md`](bot_it/COMPREHENSIVE_PROJECT_REPORT.md) للتقارير الإنجليزية

---

**تاريخ التقرير**: 2026-02-02  
**الإصدار**: 3.0.0  
**الحالة**: ✅ نشط ومتاح للاستخدام
