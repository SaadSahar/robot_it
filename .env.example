# ==========================================
# Voice Chatbot - Environment Configuration
# Gemini Live API: Direct PCM Audio Streaming
# ==========================================

# Google Cloud Service Account Credentials
# Path to your service account JSON key file
# Option 1: Set environment variable (recommended for production)
# Option 2: Place file at bot_it/credentials/service-account-key.json (automatic fallback)
# Example (Windows): set GOOGLE_APPLICATION_CREDENTIALS=D:\path\to\service-account-key.json
# Example (Linux/Mac): export GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account-key.json
GOOGLE_APPLICATION_CREDENTIALS=./ser_api.json

# Google Cloud Project Configuration
# Get your Project ID from: https://console.cloud.google.com/projectselector2/home/dashboard
# IMPORTANT: Replace with your actual Google Cloud Project ID
GOOGLE_CLOUD_PROJECT_ID=your_project_id_here

# Google Cloud Region (for Gemini Live API)
# Supported regions: us-central1, us-east1, europe-west1, asia-southeast1
GOOGLE_CLOUD_REGION=us-central1

# Google Cloud API Key (optional, for REST API fallback)
# Get your API key from: https://console.cloud.google.com/apis/credentials
GOOGLE_CLOUD_API_KEY=your_api_key_here

# Google AI Studio API Key (for Gemini API)
# Get your API key from: https://aistudio.google.com/apikey
# Note: This is the same as GOOGLE_CLOUD_API_KEY for this project
GOOGLE_API_KEY=your_api_key_here

# Gemini Live API Configuration
# Model ID for Gemini Live API
GEMINI_LIVE_MODEL=gemini-live-2.5-flash-native-audio

# Voice Configuration
# Available voices: Charon, Puck, Fenrir, Kore, etc.
GEMINI_VOICE_NAME=Charon

# Language Code
# Options: ar-EG (Arabic Egypt), ar-SA (Arabic Saudi), en-US (English), etc.
GEMINI_LANGUAGE_CODE=ar-EG

# Legacy Gemini Model (for fallback, not used in Live API)
GEMINI_MODEL=gemini-1.5-flash

# TTS Configuration (DEPRECATED - Not used with Gemini Live API)
TTS_LANGUAGE=ar-XA
TTS_VOICE=ar-XA-Wavenet-B
TTS_GENDER=MALE

# Wake Word Configuration
# The bot will only respond if the user's message starts with this word
WAKE_WORD=روبوت

# Server Configuration
PORT=3004

# Debug Configuration
# Set to 'true' to enable detailed logging
DEBUG_MODE=true

# ==========================================
# Setup Instructions:
# ==========================================
# 1. Create a Google Cloud Project
# 2. Enable Vertex AI API
# 3. Create a Service Account and download the JSON key
# 4. Place the JSON key file at: bot_it/credentials/service-account-key.json
#    OR set GOOGLE_APPLICATION_CREDENTIALS above to the full path
# 5. Update GOOGLE_CLOUD_PROJECT_ID with your actual project ID
# 6. Run: npm install
# 7. Run: npm start
#
# For detailed instructions, see: bot_it/credentials/README.md
